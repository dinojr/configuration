;; ~/.emacs.d/JCworkgroups2.el

(require 'workgroups2)

(setq wg-morph-on nil)

;; (setq wg-morph-vsteps 3)
;; (setq wg-morph-hsteps 3)

;; C-c w est le standard, mais déjà utilisé pour browse-url
(setq wg-prefix-key [f1])

(workgroups-mode 1)


